#include "func.cpp"

int main()
{
    int op = 0;
    do
    {
        op = menu();
        cout << op;

        switch (op)
        {
        case 1:

            break;
        case 2:

            break;
        case 3:

            break;
        case 4:

            break;
        default:
            break;
        }

    } while (op != 0);

    return 0;
}
