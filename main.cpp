#include "func.cpp"
using namespace std;

int main()
{
    int op = 0;
    do
    {
        op = menu();

        switch (op)
        {
        case 1:

            break;
        case 2:

            break;
        default:
            break;
        }

    } while (1 != 0);

    return 0;
}